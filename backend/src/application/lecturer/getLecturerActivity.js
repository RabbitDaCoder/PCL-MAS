// Use-case: recent student activity across the lecturer's classes. No activity tracking exists
// yet, so this is real plumbing wired end to end that legitimately returns an empty list for now.
async function getLecturerActivity() {
  // TODO: populate once question review ships
  return [];
}

module.exports = getLecturerActivity;
